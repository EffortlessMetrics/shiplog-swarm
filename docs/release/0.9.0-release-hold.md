# shiplog 0.9.0 - Release Hold

**Status:** paused
**Hold date:** 2026-05-16
**Last shipped release:** `v0.8.0`
**Candidate version metadata:** `0.9.0`
**Current decision:** keep hold ([release decision](0.9.0-release-decision.md))

## Decision

Do not tag, publish, or otherwise release `v0.9.0` now.

`v0.8.0` shipped the Evidence Repair Loop to GitHub and crates.io. The next
stretch is post-0.8 hardening and UX soak, not another immediate release train.
The review-ready packet work that landed after 0.8 stays on `main` as an
unreleased 0.9 candidate.

## Current State

- `v0.8.0` is the latest shipped release.
- PR #319 prepared 0.9.0 version metadata and release docs, but did not tag,
  publish to crates.io, or publish a GitHub release.
- `packet_quality`, Packet Readiness, claim candidates, share explain, runs
  diff, the product proof, and the guide remain available on `main`.
- Guided Setup / Doctor work also remains available on `main`: `init
  --guided`, `doctor --setup`, `sources status`, setup-blocked repair
  handoffs, share-profile setup readiness, product proof, and guide.
- Workspace package versions remain `0.9.0` as candidate metadata.
- The tag-triggered and manually dispatched release workflow runs
  `scripts/check-release-hold.sh` before release proof; while this receipt
  exists, an accidental `v0.9.0` release fails before package proof, binary
  builds, or GitHub release creation.
- Manual `release.yml` dispatch requires an explicit semver `release_tag` and
  `owner_approved_release_execution`; branch refs such as `main` are rejected
  before publish dry-run proof can start.

## Blocked Operations

While this hold is active, do not run:

- `git tag v0.9.0`
- `git push origin v0.9.0`
- `cargo publish -p shiplog --locked`
- `gh release edit v0.9.0 --draft=false --latest`
- manual `release.yml` dispatch for `v0.9.0`
- release-install smoke against `v0.9.0` assets

These operations require a fresh release-resume decision and current proof.

## Soak Focus

- dogfood the 0.8 repair loop on real review flows;
- run review-ready packet flows locally and file small bugfix PRs;
- improve wording, help, docs, and tests around packet quality;
- tighten no-write behavior for share explain and runs diff;
- keep advisory proof surfaces separate from release blocking;
- avoid new public API churn unless a real bug requires it.

## Soak Receipts

These receipts record post-0.8 hardening. They do not lift the release hold.

- #337 added the release workflow guard for held `v0.9.0` tags.
- #338 routed repairable intake reports through `shiplog repair plan` before
  direct fix commands.
- #339 tightened the review-ready packet guide from local dogfood.
- #340 pinned `shiplog open packet --latest --print-path` as no-write.
- #341 deduped repeated journal suggestions found during local-history dogfood.
- #342 recorded these post-0.8 soak receipts in release-hold docs and pinned
  them with docs-command coverage.
- #343 added a review-ready packet golden fixture for the repaired-packet
  product proof.
- #344 fixed journal-repair rerun guidance so manual evidence remains enabled
  after a repair writes to the configured manual source.
- #345 reduced repeated intake `Next:` blocks so users get one clear handoff
  plus separate open commands.
- #346 pinned `shiplog repair diff --latest` as no-write.
- #347 changed the final intake handoff to the read-only
  `shiplog share explain manager` command instead of manager rendering, which
  fails closed without a redaction key.
- #348 deduped repair-plan journal items that came from the same copyable
  manual-evidence command.
- #349 refreshed the release-hold receipts through the first post-0.8 dogfood
  fixes; the receipt refresh did not approve release execution.
- #350 printed Windows-native redaction-key setup for `share explain` recovery
  guidance while keeping manager/public rendering fail-closed.
- #351 added `repair diff` next actions for `runs diff`, `open packet`, and
  `share explain manager` after a successful repair comparison.
- #352 documented the verified repair-diff handoff and PowerShell redaction-key
  setup in the review-ready and evidence-repair guides.
- #357 refreshed RIPR lane docs so the landed stub is explicitly advisory,
  non-release-blocking, and still separate from real future analysis.
- #364 split intake output and intake report Markdown into explicit
  `Intake status` and `Packet readiness` lines after dogfood showed the older
  label could contradict `runs diff`.
- #365 applied the same readiness wording to `report validate`, `report
  summarize`, and `repair plan`, while preserving compatible reports without
  `packet_quality`.
- #367 fixed `report summarize` so the operator handoff prints read-only
  `share explain` commands and a single window date range.
- #369 hid direct manager/public share render commands from the
  `report summarize` machine-action list so the visible share route stays
  read-only until the user chooses rendering.
- #370 deduped the `intake.report.md` Window header after dogfood showed the
  date range could appear both before and inside the resolved window label.
- #371 made the intake report share handoff explain posture before rendering
  manager/public artifacts.
- #372 reduced repeated intake report fixup noise by deduping identical
  copyable `Top Fixups` commands.
- #373 aligned the v1 report schema docs with the read-only share-explain
  handoff now shown in `intake.report.md`.
- #374 aligned the crates.io README with the review-ready loop so install
  readers see the repair, quality-diff, and read-only share-explain handoff.
- #375 fixed default `./out` display in intake handoffs after Windows dogfood
  showed noisy current-directory duplication.
- #376 normalized intake and review-ready path display so Windows dogfood shows
  stable slash-separated artifact paths beside copyable handoff commands.
- #377 blocked journal repair handoffs when the configured manual journal is
  malformed, so post-0.8 dogfood points at setup repair instead of copyable
  `journal add` commands that cannot run.
- #378 made `share explain manager|public` surface packet readiness and report
  evidence debt in `Needs review`, fixing dogfood output that previously said
  `Needs review: None` for caveated packets.
- #379 reduced repeated outcome-context evidence debt so broad code-only or
  ticket-only workstreams already covered by a manual-context repair no longer
  print duplicate `journal add` guidance.
- #380 reduced first-run output noise so zero-event sources remain visible in
  source status and caveats but no longer appear as positive evidence under
  `Good`.
- #381 kept repairable intake `Next` handoffs on the repair loop by avoiding
  direct `journal add --date` commands in the top-level handoff when
  `journal add --from-repair` repair items are available.
- #382 kept repairable intake `Next` handoffs read-first by suppressing direct
  write-producing commands such as workstream splits once `repair plan` is
  available.
- #383 documented the read-first repair handoff in the review-ready and evidence
  repair guides so direct write commands in evidence debt remain contextual
  rather than first-action guidance.
- #384 aligned the rapid first-intake guide with the repair loop so missing
  manual evidence starts at `repair plan` and `journal add --from-repair`
  instead of direct journal commands.
- #385 aligned the top-level README with the read-first repair and share flow so
  write-producing commands are preceded by `repair plan`, `share explain`, and
  `share verify` guidance.
- #386 made skipped autodetected sources part of coverage metadata before packet
  rendering, so packet coverage, review output, and `share explain` agree about
  first-run source skips during post-0.8 dogfood.
- #387 kept repair and quality diffs honest after journal-only repair: source
  repair items now clear only when the newer report includes evidence from that
  source, so disappearing optional-source scope is not counted as improvement.
- #388 routed repaired rerun intake handoffs to `repair diff --latest` before
  `repair plan`, so post-repair dogfood moves into comparison instead of
  looping users back to planning first.
- #389 labeled `share explain` follow-up render commands as "Render when
  ready", so the read-only posture explanation does not look like immediate
  write-producing work during post-0.8 dogfood.
- #390 consolidated the late-soak receipts into the grouped ledger below; this
  makes the release-hold evidence easier to audit and does not approve release
  execution.
- #391 added the review-ready dogfood matrix so trusted flows and remaining
  targeted-test gaps are visible before any release-resume decision.
- #392 encoded targeted dogfood-matrix checks for public share debt, legacy
  report quality gaps, and no misleading packet-readiness improvement from
  reports without `packet_quality`.
- #393 made `share explain` name still-open source repairs from report
  receipts, so journal-only repair reruns do not make provider setup gaps look
  gone at share time.
- #394 pinned report/packet consistency checks so packet readiness, claim
  candidates, and missing-context prompts stay aligned with `packet_quality`,
  and `share explain` continues to read report receipts rather than rendered
  Markdown.
- #395 hardened old-report compatibility for partial `packet_quality` objects
  that predate claim candidates or share posture, while still prompting rerun
  for richer review-ready signals.
- #396 guarded paused release execution by requiring manual release workflow
  dispatches to name a semver tag, record owner approval, and pass the
  release-hold script before publish dry-run proof can run.
- #397 recorded a final review-ready loop dogfood transcript from local git plus
  manual repair, including repair diff, runs diff, packet readiness, claim
  candidates, read-only share explanation, and fail-closed manager verification.
- #398 recorded the release decision gate: keep the 0.9 hold active because
  owner approval and final release preflight are not present.

## Guided Setup / Doctor Receipts

These receipts record the front-door setup work that landed after the #398 hold
decision. They update the unreleased candidate scope, but they still do not lift
the release hold.

- #399 proposed Guided Setup / Doctor so setup readiness, source status,
  local-file validity, credential presence, share prerequisites, and read/write
  next actions are explicit before intake, repair, or share commands.
- #400 defined the setup readiness contract and kept setup status separate from
  source freshness, repair readiness, packet readiness, and share posture.
- #401 recorded the ADR that doctor and `sources status` are setup readiness
  surfaces, not dry-run intake engines.
- #402 added the deterministic setup readiness model without network calls or
  writes.
- #403 added read-only `shiplog doctor --setup` output over the typed model.
- #404 added `shiplog sources status` as the source-only setup projection.
- #405 added non-interactive `shiplog init --guided` defaults for local-first
  setup.
- #406 routed setup-blocked repairs through read-only doctor/source-status
  handoffs before repair action.
- #407 added manager/public share readiness to `doctor --setup` without writing
  profile artifacts.
- #408 proved guided setup prevents malformed manual-journal setup from turning
  into dead-end repair commands.
- #409 added the Guided Setup / Doctor guide for local-only, manual-only,
  token-backed GitHub, manager-share-ready, and public-share-cautious modes.
- #410 updated this release decision: Guided Setup / Doctor is part of the
  unreleased candidate scope, but `v0.9.0` remains held.
- #411 added the setup-readiness dogfood matrix so empty-directory, no-config,
  guided-config, malformed-journal, disabled-manual, token-backed,
  share-blocked, public-share, old-config/report, and Windows display flows
  have visible proof gaps and accepted caveats before any release-resume
  discussion.
- #412 added `shiplog doctor --setup --json` so agents and scripts can consume
  the same setup readiness model without scraping human CLI text.
- #413 added `contracts/schemas/setup-readiness.v1.schema.json`,
  `docs/schemas/setup-readiness-v1.md`, and setup-readiness examples so the
  doctor JSON contract is pinned while the release hold remains active.
- #414 added a doctor/source-status consistency proof so the human source-only
  view remains a projection of the setup-readiness model rather than a separate
  classifier.
- #415 added an agent-safe doctor JSON proof so malformed manual setup, missing
  provider tokens, and missing redaction setup route to read-only setup actions
  instead of evidence-repair or share-render writes.
- #416 made intake start with doctor/source-status guidance when configured
  setup is blocked, without treating optional autodetected provider skips as a
  setup-blocked lane.
- #417 proved doctor, share explain, share verify, and share rendering agree on
  manager/public redaction-key blockers without writing profile artifacts.
- #418 recorded the guided setup transcript from an empty temp workspace through
  init, doctor, doctor JSON, sources status, intake, repair, rerun, repair diff,
  runs diff, and read-only share explain.
- #419 fixed the transcript-discovered local Git identity caveat so guided
  configs with `repo = "."` can collect local Git evidence from repos without an
  origin remote.
- #420 hardened setup-readiness compatibility for older and partial setup
  states: missing optional provider tokens and missing local paths stay
  unavailable, disabled manual source state is not validated as malformed,
  unsupported config versions are the reserved `stale_config` case, and old
  report/packet artifacts do not drive doctor setup state.

## Consolidated Late-Soak Ledger

This groups the late post-0.8 dogfood fixes by the user-facing risk they
reduced. It is a release-hold audit aid, not release approval.

| Area | Receipts | What changed | Release implication |
| --- | --- | --- | --- |
| Path display | #375, #376 | Default `./out` and Windows path rendering now stay stable and copyable in intake/review-ready handoffs. | Reduces command-copy friction; does not affect release approval. |
| Unsafe manual repair handoffs | #377 | Malformed manual journals block copyable `journal add` repair handoffs and route users to setup repair instead. | Keeps repair actions honest when prerequisites are broken. |
| Share posture caveats | #378, #389 | `share explain` surfaces packet readiness and evidence debt, then labels render commands as `Render when ready`. | Keeps share posture read-first before any write-producing render command. |
| Evidence-debt noise | #379 | Outcome-context evidence debt is deduped when the same manual-context repair already covers the workstream. | Reduces repetitive "now what?" guidance without hiding caveats. |
| Zero-event evidence claims | #380 | Zero-event sources no longer appear as positive evidence under `Good`. | Prevents a weak first packet from looking cleaner than it is. |
| Read-first handoffs | #381, #382, #383, #384, #385 | Repairable flows route through `repair plan`, `journal add --from-repair`, `share explain`, and `share verify` before direct writes. | Maintains safe sequencing in the core review-ready loop. |
| Skipped-source visibility | #386 | Autodetected skipped GitHub/GitLab/Jira/Linear/JSON sources are carried into coverage, packets, review output, and share explanation. | Keeps missing source setup visible across surfaces. |
| Repair-diff truthfulness | #387 | Provider source repairs clear only when matching source evidence appears on the newer run. | Prevents misleading improvement from disappearing optional-source scope. |
| Repaired rerun handoff | #388 | A rerun after repair points to `repair diff --latest` before `repair plan`. | Moves users into comparison after repair instead of looping back to planning. |
| Release execution guard | #337, #342, #396 | Tag pushes and manual release workflow dispatches must resolve an explicit semver tag and pass `scripts/check-release-hold.sh`; manual dispatch also records owner approval. | Prevents accidental release proof, asset creation, or GitHub release creation while the hold is active. |
| Final local-history transcript | #397 | The documented dogfood run shows local git plus manual repair moving through intake, repair plan, journal repair, rerun, repair diff, runs diff, packet review, share explain, and fail-closed share verify. | Supports release confidence, but remains evidence for a later release decision rather than release approval. |
| Guided setup front door | #399-#420 | The candidate now includes setup readiness, source status, guided init defaults, setup-blocked repair routing, share setup readiness, product proof, guide, dogfood matrix, JSON/schema proof, source-status consistency, setup-aware intake handoff, share-readiness consistency, a guided setup transcript, local Git identity hardening for `repo = "."`, and old setup/config compatibility hardening. | Makes the unreleased candidate broader; still requires owner approval and current release proof before any release execution. |

The grouped ledger still leaves `v0.9.0` paused. Use it to decide what has been
soaked, not as permission to tag, publish, or create a GitHub release.

The companion
[`review-ready dogfood matrix`](../product/review-ready-dogfood-matrix.md)
tracks the flows still under soak. It is a planning/control artifact, not
release approval.

The companion
[`setup-readiness dogfood matrix`](../product/setup-readiness-dogfood-matrix.md)
tracks setup-readiness proof gaps and accepted caveats. It also does not approve
release execution.

The
[`review-ready loop transcript`](../product/review-ready-loop-transcript.md)
records the latest local-history proof run. It also does not approve release
execution.

The
[`guided setup transcript`](../product/guided-setup-transcript.md)
records the latest setup front-door proof run. It also does not approve release
execution.

The current
[`release decision`](0.9.0-release-decision.md)
keeps this hold active.

## Resume Criteria

Resume 0.9 release execution only for a concrete reason:

- a security fix;
- broken 0.8 behavior;
- a major UX bug in the shipped repair loop;
- enough validated post-0.8 value to justify another release.

A future release-resume PR should update this hold receipt, record owner
approval, rerun release proof from current `main`, and replace paused handoff
wording with copy-ready release instructions.
