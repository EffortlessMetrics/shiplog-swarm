# shiplog 0.7.0 - Crate Surface Audit

**Status:** active audit
**Date:** 2026-05-13
**Theme:** Crate-surface contraction

Related proposal:
[`SHIPLOG-PROP-0003-crate-surface-contraction`](../proposals/SHIPLOG-PROP-0003-crate-surface-contraction.md)
Related spec:
[`SHIPLOG-SPEC-0004-public-crate-support-tiers`](../specs/SHIPLOG-SPEC-0004-public-crate-support-tiers.md)
Related ADR:
[`SHIPLOG-ADR-0004-srp-modules-over-public-microcrates`](../adr/SHIPLOG-ADR-0004-srp-modules-over-public-microcrates.md)
Schema ADR:
[`SHIPLOG-ADR-0005-json-schemas-over-public-rust-schema-crate`](../adr/SHIPLOG-ADR-0005-json-schemas-over-public-rust-schema-crate.md)
Related historical receipt:
[`0.6-crate-surface-historical`](./0.6-crate-surface-historical.md)
Related implementation plan:
[`plans/crate-surface/implementation-plan.md`](../../plans/crate-surface/implementation-plan.md)

## Purpose

This audit classifies every current workspace package for the 0.7.0
crate-surface contraction lane.

It is a release control surface, not an implementation plan. It records which
packages are supported public contracts, which are transitional decisions, and
which are implementation or development support that should not be published by
0.7 release tooling.

## Current State

The workspace currently contains 13 packages:

- `shiplog`;
- 11 packages under `crates/`;
- `xtask`.

The historical 0.6.0 publish script lists 22 publishable packages: `shiplog`
plus every workspace package that existed then except `shiplog-testkit` and
`xtask`.

Current manifest posture:

- `shiplog-testkit` has `publish = false`;
- `xtask` has `publish = false`;
- `shiplog-schema` and every current `internal-module` package have
  `publish = false` in their Cargo manifests;
- `shiplog-render-md` and `shiplog-render-json` have been removed as workspace
  packages and inlined as owner modules for the first contraction slice;
- `policy/publish-allowlist.toml` limits the 0.7 release publish order to
  `shiplog` by default;
- `shiplog-ingest-git` has been removed as a workspace package and inlined as
  an owner module for the first ingest-adapter contraction slice;
- `shiplog-ingest-linear` has been removed as a workspace package and inlined
  as an owner module for the second ingest-adapter contraction slice;
- `shiplog-ingest-jira` has been removed as a workspace package and inlined as
  an owner module for the third ingest-adapter contraction slice;
- `shiplog-ingest-gitlab` has been removed as a workspace package and inlined
  as an owner module for the fourth ingest-adapter contraction slice;
- `shiplog-ingest-manual` has been removed as a workspace package and inlined
  as an owner module for the fifth ingest-adapter contraction slice;
- `shiplog-ingest-github` has been removed as a workspace package and inlined
  as an owner module for the sixth ingest-adapter contraction slice;
- `shiplog-ingest-json` has been removed as a workspace package; the adapter is
  inlined as `shiplog::ingest::json`;
- `shiplog-cache` has been removed as a workspace package; cache/freshness
  support is inlined as `shiplog::cache`;
- `shiplog-coverage` has been removed as a workspace package; coverage and
  date-window support is inlined as `shiplog::coverage`;
- `scripts/package-boundary-audit.sh`, `scripts/package-proof.sh`, and
  `scripts/publish-dry-run.sh` consume that allowlist instead of hard-coding
  workspace members;
- package-boundary audit fails if a policy-disabled package is manifest
  publishable.

That is the mismatch this lane must correct. Useful internal boundaries should
not become crates.io support promises by default.

## Target Public Surface

Default supported 0.7 public surface:

```text
shiplog
```

ADR-0005 decides that `shiplog-schema` is not a 0.7 public crate. JSON schemas
under `contracts/schemas/` remain the public machine contract.

## Classification Table

| Package | Path | Current publish setting | 0.7 tier | 0.7 publish eligibility | 0.7 disposition |
| --- | --- | --- | --- | --- | --- |
| `shiplog` | `apps/shiplog` | unset | `public-supported` | yes | Keep as the supported CLI/package surface. |
| `shiplog-schema` | `crates/shiplog-schema` | `false` | `internal-module` | no | Keep Rust schema types internal; JSON schemas are the public machine contract. |
| `shiplog-ids` | `crates/shiplog-ids` | `false` | `internal-module` | no | Inline or make unpublished support; 0.6 versions become historical. |
| `shiplog-ports` | `crates/shiplog-ports` | `false` | `internal-module` | no | Keep private until a plugin or adapter API is deliberately specified. |
| `shiplog-engine` | `crates/shiplog-engine` | `false` | `internal-module` | no | Inline product orchestration support. |
| `shiplog-coverage` | `apps/shiplog/src/coverage` | n/a | `historical-0.6` / `internal-module` | no | 0.6 crate collapsed into the `shiplog` package as coverage and date-window support. |
| `shiplog-workstreams` | `crates/shiplog-workstreams` | `false` | `internal-module` | no | Inline workstream support. |
| `shiplog-redact` | `crates/shiplog-redact` | `false` | `internal-module` | no | Inline redaction support while preserving fail-closed behavior. |
| `shiplog-bundle` | `crates/shiplog-bundle` | `false` | `internal-module` | no | Inline bundle support. |
| `shiplog-cache` | `apps/shiplog/src/cache` | n/a | `historical-0.6` / `internal-module` | no | 0.6 crate collapsed into the `shiplog` package as cache/freshness support. |
| `shiplog-render-md` | `apps/shiplog/src/render/md` | n/a | `historical-0.6` / `internal-module` | no | 0.6 crate collapsed into the `shiplog` package as Markdown rendering modules. |
| `shiplog-render-json` | `crates/shiplog-engine/src/artifact_json.rs` | n/a | `historical-0.6` / `internal-module` | no | 0.6 crate collapsed into the engine artifact writer for this contraction slice. |
| `shiplog-ingest-github` | `apps/shiplog/src/ingest/github.rs` | n/a | `historical-0.6` / `internal-module` | no | 0.6 crate collapsed into the `shiplog` package as the GitHub ingest module; recorded fixture proof moved with it. |
| `shiplog-ingest-git` | `apps/shiplog/src/ingest/git.rs` | n/a | `historical-0.6` / `internal-module` | no | 0.6 crate collapsed into the `shiplog` package as the local git ingest module. |
| `shiplog-ingest-json` | `apps/shiplog/src/ingest/json.rs` | n/a | `historical-0.6` / `internal-module` | no | 0.6 crate collapsed into the `shiplog` package as the JSON import adapter and JSONL event parser. |
| `shiplog-ingest-manual` | `apps/shiplog/src/ingest/manual` | n/a | `historical-0.6` / `internal-module` | no | 0.6 crate collapsed into the `shiplog` package as the manual evidence ingest module. |
| `shiplog-ingest-gitlab` | `apps/shiplog/src/ingest/gitlab.rs` | n/a | `historical-0.6` / `internal-module` | no | 0.6 crate collapsed into the `shiplog` package as the GitLab ingest module. |
| `shiplog-ingest-jira` | `apps/shiplog/src/ingest/jira.rs` | n/a | `historical-0.6` / `internal-module` | no | 0.6 crate collapsed into the `shiplog` package as the Jira ingest module. |
| `shiplog-ingest-linear` | `apps/shiplog/src/ingest/linear.rs` | n/a | `historical-0.6` / `internal-module` | no | 0.6 crate collapsed into the `shiplog` package as the Linear ingest module. |
| `shiplog-cluster-llm` | `crates/shiplog-cluster-llm` | `false` | `internal-module` | no | Inline optional LLM support. |
| `shiplog-team` | `crates/shiplog-team` | `false` | `internal-module` | no | Inline team support. |
| `shiplog-merge` | `crates/shiplog-merge` | `false` | `internal-module` | no | Inline merge support. |
| `shiplog-testkit` | `crates/shiplog-testkit` | `false` | `dev-only` | no | Keep test support unpublished. |
| `xtask` | `xtask` | `false` | `dev-only` | no | Keep repository automation unpublished. |

## Immediate Mismatches

The audit identifies current-state gaps for follow-up PRs:

1. The historical 0.6 publish script lists implementation crates.
2. The renderer, ingest adapter, cache, and coverage crates have been collapsed,
   but most remaining internal packages are still separate workspace packages.
3. `shiplog` still has normal path dependencies on internal workspace packages
   until those packages are collapsed into owner modules or otherwise removed
   from the public install dependency graph.
4. `shiplog-schema` still needs code movement or retained unpublished support
   before release prep, but its public-support decision is closed by ADR-0005.

This document names the gaps so implementation and release-tooling PRs have a
stable target.

## Required Proof By Change Type

### Docs And Planning PRs

```bash
cargo xtask check-policy-schemas
cargo xtask check-file-policy --mode blocking-allowlist
git diff --check
```

### Render Collapse PRs

```bash
cargo test -p shiplog --test render_md
cargo test -p shiplog-engine --test render_json
cargo test -p shiplog --test cli_integration -- report
cargo test -p shiplog --test intake_cold_start
git diff --check
```

Rendered Markdown and JSON output shape must remain unchanged unless a separate
spec-backed behavior PR changes it.

### Ingest Adapter Collapse PRs

```bash
cargo test -p shiplog --test intake_cold_start
cargo test -p shiplog --test front_door_first_pack_smoke
cargo test -p shiplog ingest::github::tests::recorded_http_fixtures_prove_full_fresh_then_cached_ingest
cargo test -p shiplog --test ingest_json
git diff --check
```

Source identity, skipped freshness, stale freshness, and recorded GitHub
fixture behavior must remain unchanged. After the GitHub adapter is collapsed,
the same fixture proof should move to the new package/module location rather
than disappear.

### Product And Trust Support Collapse PRs

Use the smallest relevant subset first, then broaden when the touched surface
crosses report, cache, redaction, bundle, or CLI behavior:

```bash
cargo test -p shiplog --test intake_cold_start
cargo test -p shiplog --test front_door_first_pack_smoke
cargo test -p shiplog --test cli_integration -- intake
cargo test -p shiplog --test cli_integration -- report
cargo test -p shiplog --test cache
cargo test -p shiplog --test cache_expiry
cargo test -p shiplog --test coverage_integration
cargo test -p shiplog --test coverage_property
cargo clippy --workspace --all-targets --all-features --locked -- -D warnings
cargo xtask check-policy-schemas
cargo xtask check-file-policy --mode blocking-allowlist
git diff --check
```

When a package named in this command set has already been collapsed, use the
equivalent test in its new module/package location and record that mapping in
the PR body.

### Release Tooling PRs

Release tooling must prove:

- the publish allowlist is explicit;
- only `public-supported` crates are publishable by default;
- `public-transitional` crates require a named exception;
- internal, dev-only, and historical crates cannot enter the 0.7 publish
  sequence accidentally;
- policy-disabled workspace packages have `publish = false` in their Cargo
  manifests;
- `cargo install shiplog` remains the user path.

Current control surface:

```bash
scripts/package-boundary-audit.sh
scripts/publish-dry-run.sh
```

## Compatibility Notes

0.6.0 implementation crates are historical/transitional. Do not yank them as
routine cleanup. The exact 0.6 crate list and forward support posture are
recorded in
[`0.6-crate-surface-historical`](./0.6-crate-surface-historical.md).

0.7.0 should stop publishing unsupported implementation surfaces forward. If a
real external Rust consumer depends on a non-CLI crate, record that fact and
promote the crate through an ADR instead of treating the 0.6 publication as
implicit proof of support.

## Open Decisions

- Decide when `shiplog-schema` is collapsed or retained as unpublished
  workspace support.
- Decide whether collapsed crates are removed from the workspace immediately or
  remain as unpublished transitional packages while dependent modules are moved.
- Continue collapsing or marking non-public implementation packages after the
  release tooling guardrail.

## Non-Goals

This audit does not:

- change first-run behavior;
- change report schemas;
- start Evidence Repair Loop work;
- create a plugin or adapter API;
- yank 0.6.0 crates.
