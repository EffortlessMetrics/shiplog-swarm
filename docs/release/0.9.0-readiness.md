# shiplog 0.9.0 - Paused Release Readiness

**Planned tag:** `v0.9.0` (not created)
**Date:** 2026-05-15
**Status:** paused after the shipped `v0.8.0` release
**Theme:** Review-ready packet quality

This readiness ledger now tracks an unreleased 0.9.0 candidate after the
review-ready packet quality lane. The release hold receipt is
[`0.9.0-release-hold.md`](0.9.0-release-hold.md), and the short handoff is
[`RELEASE_HANDOFF_0.9.0.md`](../../RELEASE_HANDOFF_0.9.0.md).

## Prepared State

`v0.9.0` is a paused, unreleased candidate. PR #319 prepared version metadata
and release docs, but the post-0.8 decision is to harden and soak before
release execution.

- Tag: not created yet.
- GitHub release: not created yet.
- crates.io: not published yet.
- Release workflow: not run yet.
- Release workflow guard: `scripts/check-release-hold.sh` blocks `v0.9.0` while
  [`0.9.0-release-hold.md`](0.9.0-release-hold.md) exists.
- Previous release checkpoint: `v0.8.0`.
- Release execution: blocked by
  [`0.9.0-release-hold.md`](0.9.0-release-hold.md).

## Candidate Thesis

If resumed, shiplog 0.9.0 would make a repaired packet review-ready. The 0.8
Evidence Repair Loop made rough packets improvable; the current candidate adds
conservative quality surfaces on top of that loop: packet readiness, evidence
strength, receipt-backed claim candidates, missing-context prompts, share
posture explanation, quality diffs, and a worked guide.

The candidate does not write performance-review prose, score employees, require
LLMs, mutate provider records, or turn share profiles into dashboards. It gives
the user a defensible scaffold and shows what still needs human context.

## Candidate Contents

- `intake.report.json` includes optional compatible v1 `packet_quality` with
  packet readiness, evidence strength, claim candidates, and share posture
  compatibility fields.
- `packet.md` opens with `Packet Readiness` and renders `Claim Candidates` when
  receipts support them.
- `shiplog share explain manager|public` explains included, removed, blocked,
  and needs-review share posture without requiring a redaction key or writing
  profile artifacts.
- `shiplog runs diff --latest` compares packet quality movement across recent
  runs.
- `docs/guides/review-ready-packet.md` teaches the full collect, repair, rerun,
  compare, interpret, share flow.
- The front-door product proof now covers repaired packet readiness, claim
  candidates, quality diff, and share posture explanation.

## Scope Summary

| Layer | Status |
| --- | --- |
| Review-ready packet proposal (#307) | merged |
| Packet quality and claim candidate spec (#308) | merged |
| Claim-candidate boundary ADR (#309) | merged |
| Packet evidence strength model (#311) | merged |
| Packet readiness rendering (#312) | merged |
| Receipt-backed claim candidates (#313) | merged |
| Claim candidate packet rendering (#314) | merged |
| Share posture explanation (#315) | merged |
| Packet quality diff across runs (#316) | merged |
| Product proof (#317) | merged |
| Worked guide (#318) | merged |
| Release prep (#319) | merged |
| Release hold | active |

## Post-0.8 Soak Evidence

This section records hardening evidence gathered after `v0.8.0` shipped. It is
not release approval, and it does not replace the pre-resume checklist.

- #337 made release tooling visibly safe by blocking held `v0.9.0` tags while
  the release-hold receipt exists.
- #338 improved next-action clarity: repairable intake reports now route users
  through `shiplog repair plan --out ... --latest`.
- #339 updated the review-ready guide from real local-history dogfood, including
  source-backed packets that still need curation and advisory repair-plan rows.
- #340 strengthened no-write proof for `shiplog open packet --latest
  --print-path`.
- #341 reduced repetitive packet/report guidance by deduping repeated journal
  suggestions discovered in local-history dogfood.
- #342 made the release-hold receipts themselves durable with docs-command
  coverage; this evidence is not release approval.
- #343 added a review-ready packet golden fixture for the repaired-packet
  product proof.
- #344 fixed journal-repair rerun guidance so manual evidence stays enabled
  after `journal add --from-repair` writes local evidence.
- #345 reduced duplicate intake next-action blocks and kept open commands
  separate from the main repair/share handoff.
- #346 strengthened no-write proof for `shiplog repair diff --latest`.
- #347 changed the final intake handoff to read-only
  `shiplog share explain manager` before any manager share rendering.
- #348 deduped repair-plan journal repair items that came from the same
  copyable manual-evidence command.

## User-Facing Contract

- `shiplog intake --last-6-months --explain` still creates a first packet and
  durable report from an empty directory.
- `shiplog repair plan --latest` and `shiplog journal add --from-repair
  <repair_id>` remain the safe local repair path.
- `shiplog repair diff --latest` shows repair-state movement by stable
  `repair_key`.
- `shiplog runs diff --latest` shows packet-quality movement across reruns.
- `packet.md` can explain readiness and claim candidates without inventing
  impact or final review prose.
- `shiplog share explain manager|public --latest` is read-only and may be used
  before a redaction key exists.
- Manager/public rendering still fails closed without `--redact-key` or
  `SHIPLOG_REDACT_KEY`.

## Pre-Resume Checklist

- [ ] Release hold explicitly lifted by a follow-up release-resume PR or owner
  instruction.
- [ ] Post-0.8 soak evidence reviewed.
- [ ] `main` CI green for `Check (ubuntu-latest)`, `Check (windows-latest)`,
  `Policy gates`, `cargo-deny`, smoke lanes, Codecov/advisory status, and Droid
  review.
- [ ] Package versions are all `0.9.0`.
- [ ] Package boundary audit reports one publish-allowed package: `shiplog`.
- [ ] Publish dry-run publishes only `shiplog`.
- [ ] `cargo publish -p shiplog --dry-run --locked` passes.
- [ ] Product proof passes.
- [ ] Local cargo-install smoke reports `shiplog 0.9.0`.
- [ ] File/policy/executable/no-panic gates pass.

## Release-Prep Proof Commands

These commands were useful for PR #319. Treat them as stale while the release
hold is active. A future release-resume PR must rerun current proof on merged
`main` before tagging:

```bash
bash scripts/package-version-audit.sh
bash scripts/package-boundary-audit.sh
bash scripts/publish-dry-run.sh
cargo test -p shiplog --test front_door_first_pack_smoke
cargo test -p shiplog --test docs_commands
cargo test -p shiplog --test cli_integration -- share
cargo test -p shiplog --test cli_integration -- runs
cargo clippy --workspace --all-targets --all-features --locked -- -D warnings
cargo xtask check-policy-schemas
cargo xtask check-file-policy --mode blocking-allowlist
cargo xtask check-executable-files --mode blocking-allowlist
cargo xtask check-no-panic-family --mode blocking-allowlist
git diff --check
```

On Windows, run the shell scripts from Git Bash or another bash-capable shell.
For the local cargo-install smoke before release assets exist:

```powershell
cargo install --path apps/shiplog --locked --root target/release-install-local --force
target\release-install-local\bin\shiplog.exe --version
target\release-install-local\bin\shiplog.exe intake --help
target\release-install-local\bin\shiplog.exe runs diff --help
target\release-install-local\bin\shiplog.exe share explain manager --help
```

## Post-Resume Asset Smoke

Release-install smoke needs release assets, so it is post-tag and blocked while
the hold is active:

```bash
scripts/release-install-smoke.sh v0.9.0
scripts/verify-release.sh v0.9.0
```

On Windows PowerShell:

```powershell
scripts\release-install-smoke.ps1 -Version v0.9.0
```

## Paused Tag And GitHub Release

Do not create or push `v0.9.0` while the hold is active.

A future release-resume PR should replace this section with fresh copy-ready
commands after current proof has passed. The tag-triggered `release.yml` flow
should then build multi-platform binaries, create the release, upload assets,
generate checksums, and run release validation plus release-mode integration
tests.

## Publish To crates.io

Do not publish `shiplog 0.9.0` while the hold is active. Publishing still
requires explicit approval and a Cargo registry credential after a future
release-resume decision.

Do not publish `shiplog-testkit` or `xtask`; they remain `publish = false`.

## Publish GitHub Release

Do not create, undraft, or mark latest a 0.9.0 GitHub release while the hold is
active. After a future release resume, confirm the release is non-draft,
non-prerelease, and marked latest only after `release.yml` uploads assets and
crates.io publish completes.

## Known Non-Blockers

- 0.9 does not add provider mutation, OAuth, dashboards, GUI/TUI, plugin APIs,
  or LLM-written prose.
- Public share verification remains a guardrail, not a guarantee of perfect
  privacy.
- Historical 0.6 implementation crates remain available as historical artifacts;
  0.9 continues the 0.7 supported forward public surface of `shiplog` only.

## Rollback Path

- While held: forward-fix release notes, version metadata, or hold receipts in
  a normal PR. Do not delete tags or releases because none should exist for
  0.9.0 while the hold is active.
- Before a future tag: revert the release-prep PR or forward-fix release notes
  and version metadata.
- After tagging but before crates.io publish: delete `v0.9.0` locally and on
  origin, then rerun release prep.
- After crates.io publish: do not delete or rewrite the release. Ship a
  forward-fix patch/minor release and mark the GitHub release notes with the
  known issue.

## Owner

`EffortlessMetrics`. Release driver: project owner.
