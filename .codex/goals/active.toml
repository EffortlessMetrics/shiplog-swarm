schema_version = 1

id = "shiplog-swarm-control-plane"
title = "Shiplog swarm development control plane"
status = "active"
owner = "codex"
created = "2026-05-22"

objective = """
Keep normal shiplog development moving through EffortlessMetrics/shiplog-swarm
while EffortlessMetrics/shiplog remains the release/public source surface.
Use focused swarm PRs, green normalized CI, regular merge-commit promotions,
and repo-native receipts so the next agent can continue without chat memory.
"""

end_state = [
  "Normal development targets EffortlessMetrics/shiplog-swarm.",
  "Swarm PRs stay focused, validated, squash-merged, and cleaned up.",
  "Shiplog Rust Small Result remains the normalized swarm gate.",
  "EffortlessMetrics/shiplog receives proven swarm work by regular merge-commit promotion PRs.",
  "Release authority stays on EffortlessMetrics/shiplog until an explicit release cutover.",
  "Source-of-truth goals, plans, support tiers, and promotion receipts stay current.",
]

[[work_item]]
id = "promotion-cadence"
status = "active"
proposal = "SHIPLOG-PROP-0010"
spec = "SHIPLOG-SPEC-0011"
adr = "SHIPLOG-ADR-0011"
plan = "plans/shiplog-swarm/current-promotion.md"
commands = [
  "rtk git fetch origin --prune",
  "rtk git fetch swarm --prune",
  "rtk git merge-base origin/main swarm/main",
  "rtk git log --oneline origin/main..swarm/main",
  "rtk gh pr list --repo EffortlessMetrics/shiplog-swarm --state open --limit 30",
  "rtk gh pr list --repo EffortlessMetrics/shiplog --state open --limit 30",
  "rtk gh issue list --repo EffortlessMetrics/shiplog-swarm --state open --limit 30",
  "rtk gh issue list --repo EffortlessMetrics/shiplog --state open --limit 30",
  "rtk gh run list --repo EffortlessMetrics/shiplog-swarm --branch main --limit 10",
  "rtk gh run list --repo EffortlessMetrics/shiplog --branch main --limit 10",
  "rtk cargo xtask repo-contract-report",
  "rtk git diff --check",
]

# Historical promotion receipts through source PR #607 remain in
# plans/shiplog-swarm/implementation-plan.md and the Git history of this file.
# The active manifest intentionally indexes only the current promotion slice.
receipts = [
  "EffortlessMetrics/shiplog-swarm#225",
  "EffortlessMetrics/shiplog-swarm#226",
  "EffortlessMetrics/shiplog-swarm#233",
  "EffortlessMetrics/shiplog#652",
  "EffortlessMetrics/shiplog-swarm#200 closed as stale pre-reconciliation evidence",
  "EffortlessMetrics/shiplog#632 closed as deferred source-only dependency work",
  "EffortlessMetrics/shiplog#648 closed as unused transport bootstrap",
  "EffortlessMetrics/shiplog#649 closed after pre-receipt source proof because its standalone-repository head was pinned",
  "EffortlessMetrics/shiplog#650 closed as superseded by stable source-local promotion #652",
  "EffortlessMetrics/shiplog#651 closed as unused issue-to-PR transport handle",
]
